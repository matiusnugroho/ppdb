{
    "private": true,
    "type": "module",
    "scripts": {
        "serve": "php artisan serve",
        "vue": "cross-env-shell \"cd front_end && npm run dev -- --open\"",
        "dev": "concurrently --names \"LARAVEL,FRONTEND\" -c \"blue.bold,green.bold\" \"npm run serve\" \"npm run vue\""
    },
    "devDependencies": {
        "axios": "^1.6.4",
        "concurrently": "^9.2.1",
        "cross-env": "^10.1.0",
        "laravel-vite-plugin": "^1.0",
        "vite": "^5.0"
    }
}
